import { EmployeeAccountDetails } from './employee-account-details';

describe('EmployeeAccountDetails', () => {
  it('should create an instance', () => {
    expect(new EmployeeAccountDetails()).toBeTruthy();
  });
});
