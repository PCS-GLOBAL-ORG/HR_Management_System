import { EmployeeJobDetails } from './employee-job-details';

describe('EmployeeJobDetails', () => {
  it('should create an instance', () => {
    expect(new EmployeeJobDetails()).toBeTruthy();
  });
});
