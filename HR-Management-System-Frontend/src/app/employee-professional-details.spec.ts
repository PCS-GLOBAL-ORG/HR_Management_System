import { EmployeeProfessionalDetails } from './employee-professional-details';

describe('EmployeeProfessionalDetails', () => {
  it('should create an instance', () => {
    expect(new EmployeeProfessionalDetails()).toBeTruthy();
  });
});
