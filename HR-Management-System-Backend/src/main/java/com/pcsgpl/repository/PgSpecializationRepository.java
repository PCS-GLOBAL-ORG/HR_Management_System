package com.pcsgpl.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.pcsgpl.entity.PgSpecialization;

public interface PgSpecializationRepository extends JpaRepository<PgSpecialization, Integer>{

}